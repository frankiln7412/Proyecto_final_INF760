# Servidor
PORT=5000
NODE_ENV=development

# Base de datos (local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=inventario

# O usar DATABASE_URL (para Railway, Render, etc.)
# DATABASE_URL=postgresql://user:pass@host:5432/db

# JWT
JWT_SECRET=mi_super_secreto_jwt_2026
JWT_EXPIRES_IN=8h

# Cloudinary (para imágenes persistentes en producción)
# CLOUDINARY_CLOUD_NAME=tu_cloud_name
# CLOUDINARY_API_KEY=tu_api_key
# CLOUDINARY_API_SECRET=tu_api_secret

# CORS (separar múltiples orígenes con coma)
# CORS_ORIGIN=https://tuapp.railway.app
